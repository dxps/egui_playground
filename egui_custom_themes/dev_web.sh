
## Notes: 
##   Use `trunk serve -v --poll` for having the debug level of trunk.
##   Use `trunk serve -vv --poll` for having the trace level of trunk.
##   Alternatively, use:
##       `trunk serve --log-level=debug --poll`.
##       `trunk serve --log-level=trace --poll`.

trunk serve --poll

